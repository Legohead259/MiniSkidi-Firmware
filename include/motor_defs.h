enum MotorNums {
    RIGHT_MOTOR = 1,
    LEFT_MOTOR,
    ARM_MOTOR,
};

enum ButtonNumbers {
    UP = 1,
    DOWN,
    LEFT,
    RIGHT,
    ARMUP,
    ARMDOWN,
    STOP
};

enum MotorDirections {
    FORWARD = 1,
    BACKWARD = -1
};